#include "common.h"

class Solution189 {
public:
    void rotate(vector<int>& nums, int k) {
        
        for (int i = 0; i < nums.size(); i++)
        {
            /* code */
        }
        
    }
};