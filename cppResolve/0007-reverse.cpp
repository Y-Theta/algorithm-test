#include "0-common.h"

class Solution7 {
public:
    int reverse(int x) {
        int digit = 0;
        while (x != 0)
        {
            digit++;
            x = x / 10;
        }
        
        
    }
};